import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {flex: 1, backgroundColor: '#eaeaea', padding: 10},
  inner_container: {
    backgroundColor: '#fff',
    marginBottom: 10,
    borderRadius: 10,
  },
  button_container: {
    alignItems: 'center',
    padding: 10,
    borderRadius: 10,
  },
});
